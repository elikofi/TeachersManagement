@model IEnumerable<TeachersManagementApp.Models.Domain.Role>
@{
}

<div class="container">
    <a href="/Role/Add" class="btn btn-outline-warning">Add new role</a>
</div>

<div class="container pt-10">
    <div>
        <h2>Roles</h2>
        <table class="table table-stripped table-bordered">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.RoleName</td>
                        <td>
                            <a class="btn btn-outline-primary" href="/Role/Update?id=@item.Id">Edit</a>
                            <a class="btn btn-outline-danger" onclick="return window.confirm('Are you sure')" href="/Role/Delete?id=@item.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
