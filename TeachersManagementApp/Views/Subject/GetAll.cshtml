@model IEnumerable<TeachersManagementApp.Models.Domain.Subject>
@{
}

<div class="container">
    <a href="/Subject/Add" class="btn btn-outline-warning">Add new subject</a>
</div>

<div class="container pt-10">
    <div>
        <h2>Subjects</h2>
        <table class="table table-stripped table-bordered">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.SubjectName</td>
                        <td>
                            <a class="btn btn-outline-primary" href="/Subject/Update?id=@item.Id">Edit</a>
                            <a class="btn btn-outline-danger" onclick="return window.confirm('Are you sure')" href="/Subject/Delete?id=@item.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
