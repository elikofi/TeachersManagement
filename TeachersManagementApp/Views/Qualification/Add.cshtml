@model TeachersManagementApp.Models.Domain.Qualification
@{
}

<div class="container pt-10">
    <div>
        <h2>Add Qualification</h2>
        <form asp-action="Add" method="post">
            <div class="form-group">
                <label asp-for="QualificationName">Qualification Type:</label>
                <input type="text" class="form-control" asp-for="QualificationName" />
                <span asp-validation-for="QualificationName" class="text-danger"></span>
            </div>

            @if (TempData["msg"] != null)
            {
                <div class="form-group">
                    @TempData["msg"]
                </div>
            }
            <div class="form-group pt-3">
                <button type="submit" class="btn btn-outline-success">Save</button>
                <a href="/Qualification/GetAll" class="btn btn-outline-info">List</a>
            </div>
        </form>
    </div>
</div>
