@model IEnumerable<TeachersManagementApp.Models.Domain.Nationality>
@{
}

<div class="container">
    <a href="/Nationality/Add" class="btn btn-outline-warning">Add new nationality</a>
</div>

<div class="container pt-10">
    <div>
        <h2>Nationalities</h2>
        <table class="table table-stripped table-bordered">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.NationalityName</td>
                        <td>
                            <a class="btn btn-outline-primary" href="/Nationality/Update?id=@item.Id">Edit</a>
                            <a class="btn btn-outline-danger" onclick="return window.confirm('Are you sure')" href="/Nationality/Delete?id=@item.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
